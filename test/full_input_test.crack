conlog "=== ПОЛНЫЙ ТЕСТ INPUT МОДУЛЯ ==="

imp input

conlog "1. Тест ввода строки:"
name = input.readLine("Введите ваше имя: ")
conlog "Привет,"
conlog (name)

conlog "2. Тест ввода числа:"
age = input.readNumber("Введите ваш возраст: ")
conlog "Вам лет:"
conlog (age)
