# 🚀 Crack Programming Language

**современный язык программирования с пробельным синтаксисом для *nix систем!**

## О языке Crack для леймов / новичков

**Crack** - это специально разработанный язык программирования для **начинающих и подростков**, которые хотят изучать программирование в **непринужденной атмосфере "вайб кодинга"**. 

### 🎯 Философия языка
- **👶 Простота превыше всего** - никаких сложных конструкций
- **🎨 Вайб кодинг** - программирование должно быть веселым и творческим
- **🤖 ИИ-помощник** - автоматическое исправление ошибок для обучения
- **🐧 *nix-ориентированность** - работает только на Linux/macOS
- **📚 Образовательная цель** - изучение основ программирования

### ⚠️ Важное ограничение
**🚫 Windows не поддерживается!** Crack создан исключительно для Unix-подобных систем (Linux, macOS) и никогда не будет портирован на Windows. 

## 🌟 Почему именно *nix?
- **🔒 Безопасность** - лучший контроль над системой для детей
- **📖 Образование** - знакомство с командной строкой и Unix-философией  
- **🛠️ Инструменты** - встроенные утилиты разработки
- **🎓 Будущее** - большинство серверов работают на Linux

## ✨ Особенности для новичков

🐍 **Чистый пробельный синтаксис** - без двоеточий и запутанных скобок  
📦 **Простая модульная система** - легкое подключение готовых функций  
🤖 **ИИ-исправления** - умный помощник исправит ошибки и объяснит  
🎨 **Красивый вывод** - эмодзи и цветные сообщения  
🔧 **VSCode поддержка** - полноценная IDE с автодополнением  
👨‍👩‍👧‍👦 **Семейная безопасность** - контролируемая среда выполнения  

## 📚 Полный справочник команд

### 1. 📺 Вывод на экран - `conlog`
```crack
// Выводим текст
conlog "Привет, мир!"

// Выводим значение переменной  
name = "Вася"
conlog (name)

// Выводим число
age = 12
conlog (age)
```

### 2. 📦 Переменные - знак `=`
```crack
// Строки
message = "Это строка"
greeting = "Привет!"

// Числа
number = 42
pi = 3.14

// Математические выражения
result = 5 + 3 * 2
total = number + 10
```

### 3. 🤔 Условия - `if/else` (только отступы!)
```crack
age = 10

if (age >= 13)

  conlog "Ты подросток!"
  conlog "Можешь изучать сложные темы"
else

  conlog "Ты ребенок!"
  conlog "Начни с простых примеров"
```

### 4. 🔄 Циклы

#### Цикл while
```crack
counter = 0
while (counter < 5)

  conlog "Считаем:"
  conlog (counter)
  counter = counter + 1
```

#### Цикл for  
```crack
for (i = 0; i < 3; i = i + 1)

  conlog "Цикл номер:"
  conlog (i)
```

### 5. 📖 Импорт модулей - `imp`
```crack
// Подключаем модуль для ввода
imp input

// Используем функции модуля
name = input.readLine("Как тебя зовут? ")
conlog (name)
```

### 6. 🎯 Динамическое выполнение - `codego:()`
```crack
// Сохраняем код в переменную
mycode = "conlog \"Это код из переменной!\""

// Выполняем код
codego:(mycode) 
```
 
### 7. 💬 Комментарии - два стиля!

#### Стиль 1: `//текст//`
```crack
// Это комментарий //
conlog "Привет!"  // Это тоже комментарий //
```

#### Стиль 2: `##` (новый!)
```crack
## Это комментарий в стиле ##
conlog "Привет!"  ## Еще один комментарий

## Комментарий на отдельной строке
```

### 8. 🔢 Операторы сравнения
```crack
a = 5
b = 3

if (a > b)    // больше

  conlog "a больше b"

if (a < b)    // меньше

  conlog "a меньше b"

if (a == b)   // равно

  conlog "a равно b"

if (a != b)   // не равно

  conlog "a не равно b"

if (a >= b)   // больше или равно

  conlog "a больше или равно b"

if (a <= b)   // меньше или равно

  conlog "a меньше или равно b"
```

### 9. 🧠 Логические операторы
```crack
age = 15
hasPermission = true

if (age >= 13 && hasPermission)  // И

  conlog "Доступ разрешен!"

if (age < 10 || age > 18)        // ИЛИ

  conlog "Особый случай"

if (!hasPermission)              // НЕ

  conlog "Доступ запрещен"
```

## 📦 CRAPM - Детальное руководство

**CRAPM (Crack Package Manager)** - это система управления модулями для языка Crack.

### 🔧 Основные команды

#### Установка модуля
```bash
crapm install input
# Загружает модуль input с автоустановкой зависимостей

crapm install math  
# Загружает математические функции
```

#### 🚀 Автоматическая установка зависимостей
```bash
crapm install input
# Автоматически устанавливает модуль input + все его зависимости
# Никаких дополнительных команд не нужно!
```

#### Просмотр модулей
```bash
crapm list
# Показывает установленные модули

crapm list-available
# Показывает доступные для установки модули
```

#### Удаление модуля
```bash
crapm uninstall input
# Удаляет модуль input
```

### 📋 Доступные модули для детей

| Модуль | Описание | Функции |
|--------|----------|---------|
| **input** | Ввод данных | `readLine()`, `readNumber()`, `confirm()` |
| **math** | Математика | `add()`, `subtract()`, `multiply()`, `divide()` |
| **string** | Работа с текстом | Обработка строк |
| **file** | Файлы | Чтение и запись файлов |
| **http** | Интернет | HTTP запросы |
| **crypto** | Шифрование | Простые алгоритмы шифрования |

### 🌐 Как работает CRAPM

1. **Загрузка с GitHub** - модули берутся из официального репозитория
2. **Автоматическая установка** - создается папка `crack_modules/`
3. **Проверка зависимостей** - `package.json` и `info.json`
4. **Автоустановка npm зависимостей** - npm install выполняется автоматически  
5. **Автоустановка Crack модулей** - зависимые модули ставятся рекурсивно
6. **Умные ошибки** - подсказки при проблемах с сетью

```
📁 Структура модуля:
crack_modules/
  └── input/
      ├── info.json      # Описание модуля
      ├── package.json   # Зависимости
      └── src/
          └── index.js   # Код модуля
```

## 🚀 Установка и запуск

### 📋 Системные требования

✅ **Поддерживаемые системы:**
- 🐧 **Linux** (Ubuntu, Fedora, Debian, Arch и др.)
- 🍎 **macOS** (все версии)

❌ **НЕ поддерживается:**
- 🚫 **Windows** (никогда не будет поддерживаться!)

✅ **Необходимые программы:**
- **Node.js** (версия 16+)
- **npm** (обычно идет с Node.js)
- **TypeScript** (для разработки)
- **Git** (для клонирования)

### 🛠️ Быстрая установка

```bash
# 1. Клонируем репозиторий
git clone https://github.com/derxanax/crack.git
cd crack

# 2. Устанавливаем зависимости
npm install

# 3. Компилируем TypeScript → JavaScript
npm run build

# 4. Устанавливаем основные модули (зависимости ставятся автоматически!)
node dist/crapm.js install input
node dist/crapm.js install math

# 5. Готово! Запускаем первую программу
echo 'conlog "Привет, Crack!"' > hello.crack
node dist/main.js hello.crack
```

### 🎯 Автоматическая установка
```bash
# Запускаем скрипт автоустановки
chmod +x start.sh
./start.sh
```

## 🔧 Техническая архитектура

### 🧠 Как работает интерпретатор

```
📄 Crack код (.crack)
    ↓
🔍 Токенизация (Lexer)
    ↓  
🌳 Синтаксический анализ (Parser)
    ↓
🎯 AST дерево (Abstract Syntax Tree)
    ↓
⚡ Выполнение (Executor)
    ↓
📺 Результат
```

### 🎫 Типы токенов
- **IDENTIFIER** - имена переменных (`name`, `age`)
- **STRING** - строки (`"Привет"`)
- **NUMBER** - числа (`42`, `3.14`)
- **KEYWORD** - ключевые слова (`conlog`, `if`, `while`)
- **OPERATOR** - операторы (`+`, `-`, `=`)
- **COMPARISON** - сравнения (`==`, `>`, `<`)
- **LOGICAL** - логические (`&&`, `||`, `!`)
- **INDENT/DEDENT** - отступы (как в Python, БЕЗ двоеточий!)

### 🤖 ИИ-интеграция

Crack может автоматически исправлять ошибки через локальный ИИ API:

```bash
# Запустите ИИ API на порту 3700
# При ошибке Crack предложит исправление
```

**Примеры исправлений:**
- `conlog (` → `conlog "Привет"`  
- Незакрытые кавычки → Автодобавление `"`
- Неправильные отступы → Исправление структуры

### 📁 Структура проекта

```
crack/
├── 📂 src/
│   ├── main.ts          # 🧠 Основной интерпретатор
│   └── crapm.ts         # 📦 Менеджер пакетов
├── 📂 dist/             # 🏗️ Скомпилированный JavaScript
├── 📂 test/             # 🧪 Тестовые файлы
├── 📂 modules/          # 📚 Локальные модули
├── 📂 vsc-crack/        # 🔧 VSCode расширение
├── 📄 package.json      # 📋 Зависимости Node.js
├── 📄 tsconfig.json     # ⚙️ Настройки TypeScript
└── 📄 .gitignore        # 🚫 Игнорируемые файлы
```

## 🧪 Примеры для изучения

### 👶 Для начинающих (5-8 лет)
```crack
conlog "Меня зовут Робот!"
conlog "Мне нравится считать:"
conlog "1 + 1 = 2"

name = "Маша"
conlog "Привет,"
conlog (name)
```

### 🧒 Для детей (9-12 лет)
```crack
age = 10

if (age < 13)

  conlog "Ты ребенок!"
  conlog "Изучай программирование постепенно"
else

  conlog "Ты подросток!"
  conlog "Можешь изучать сложные темы"

// Простой цикл //
counter = 1
while (counter <= 3)

  conlog "Считаем:"
  conlog (counter)
  counter = counter + 1
```

### 🎓 Для подростков (13+ лет)
```crack
imp input
imp math

conlog "🔢 Калькулятор на Crack!"

num1 = input.readNumber("Введите первое число: ")
num2 = input.readNumber("Введите второе число: ")

sum = math.add(num1, num2)
product = math.multiply(num1, num2)

conlog "Сумма:"
conlog (sum)
conlog "Произведение:"
conlog (product)
```

## 👨‍👩‍👧‍👦 Для родителей и учителей

### 🔒 Безопасность
- **Изолированная среда** - код выполняется в контролируемом окружении
- **Нет доступа к системе** - невозможно навредить компьютеру
- **Только *nix системы** - дополнительная безопасность
- **Локальные модули** - проверенные компоненты

### 📚 Образовательные цели
- **Алгоритмическое мышление** - через простые конструкции
- **Логика программирования** - условия, циклы, переменные
- **Знакомство с командной строкой** - Unix-навыки
- **Основы модульности** - импорт и использование библиотек

### 🎯 Возрастные группы
- **5-8 лет**: Простые команды `conlog`, переменные
- **9-12 лет**: Условия `if/else`, простые циклы
- **13+ лет**: Модули, сложная логика, проекты

### 🛠️ Настройка для школы/дома
1. **Установите Linux** (рекомендуется Ubuntu)
2. **Создайте учетную запись** для ребенка
3. **Установите Crack** по инструкции выше
4. **Настройте VSCode** с расширением
5. **Начните с простых примеров**

## 🎨 VSCode расширение

### 🌟 Возможности для детей
- 🎨 **Красивая подсветка** синтаксиса с детскими цветами
- 💡 **Умные подсказки** - автодополнение функций модулей
- 🔥 **Динамическая загрузка** - модули подгружаются автоматически
- 🔄 **Живое обновление** - изменения видны сразу
- 🚨 **Дружелюбные ошибки** - понятные сообщения для детей

### 📦 Установка расширения
```bash
cd vsc-crack
npm install
npm run compile

# Откройте VSCode и установите расширение
# Откройте файл .crack для тестирования
```

## 🤖 ИИ-помощник

Crack интегрирован с локальным ИИ для обучения детей:

### 🔧 Настройка ИИ API
```bash
# Запустите локальный ИИ сервер на порту 3700
# При ошибках Crack предложит автоисправление
```

### 🎯 Что исправляет ИИ
- ❌ `conlog (` → ✅ `conlog "Привет"`
- ❌ Незакрытые кавычки → ✅ Автодобавление
- ❌ Неправильные отступы → ✅ Исправление структуры
- ❌ Опечатки в командах → ✅ Предложения исправлений

## 🐛 Отладка и помощь

### 📞 Куда обращаться
- **GitHub Issues** - для багов и предложений
- **Документация** - этот README
- **Примеры** - в папке `test/`

### 🔍 Частые проблемы
1. **"Модуль не найден"** → Установите через `crapm install`
2. **"Ошибка компиляции"** → Проверьте TypeScript версию
3. **"API не отвечает"** → Запустите ИИ сервер
4. **Windows ошибки** → Используйте Linux/macOS

## 📄 Лицензия

MIT License © 2024 derxanax

---

## 🎉 Начинаем программировать!

```bash
# Создайте свою первую программу
echo 'conlog "Я изучаю Crack!"' > my_first_program.crack
echo 'conlog "Это весело!"' >> my_first_program.crack
echo 'name = "Программист"' >> my_first_program.crack
echo 'conlog (name)' >> my_first_program.crack

# Запустите её
node dist/main.js my_first_program.crack
```

**🌟 Добро пожаловать в мир программирования с Crack! 🌟** 
